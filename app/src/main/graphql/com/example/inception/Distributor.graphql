query GetDistributor(
    $role: String!
){
    users_by_role(role: $role) {
        id
        username
        email
        role
        whatsapp
        image {
            link
        }
        looking_for
    }
}